% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reg_filiere.R
\name{reg_filiere}
\alias{reg_filiere}
\title{Régression de filière avec effets fixes, poids et erreurs groupées}
\usage{
reg_filiere(
  data,
  outcome,
  filiere_vars,
  controls = NULL,
  fixed_effects = NULL,
  weights = NULL,
  cluster = NULL
)
}
\arguments{
\item{data}{Un data frame au niveau ménage.}

\item{outcome}{Nom (chaîne) de la variable à expliquer (ex. "ln_rendement").}

\item{filiere_vars}{Vecteur de noms des variables d'intérêt
(ex. c("ln_intrants", "fertilite")).}

\item{controls}{Vecteur de noms des variables de contrôle (défaut NULL).}

\item{fixed_effects}{Vecteur de noms des effets fixes (ex. "grappe"),
ou NULL pour aucun.}

\item{weights}{Nom de la variable de poids (ex. "hhweight"), ou NULL.}

\item{cluster}{Nom de la variable de clustering des erreurs (ex. "grappe"),
ou NULL.}
}
\value{
Un objet \code{fixest} (résultat de l'estimation).
}
\description{
Estime par MCO (via \code{fixest::feols}) l'effet de variables de filière
sur un résultat, avec contrôles, effets fixes, poids d'enquête et erreurs
groupées optionnels. Sert aussi bien aux rendements qu'aux scores de
sécurité alimentaire (FIES, HDDS).
}
