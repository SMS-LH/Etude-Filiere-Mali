% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prix_chaine.R
\name{prix_chaine}
\alias{prix_chaine}
\title{Calculer ou traiter un prix unitaire le long de la chaîne de valeur}
\usage{
prix_chaine(
  data,
  col_prix = NULL,
  col_montant = NULL,
  col_quantite = NULL,
  methode_nettoyage = c("iqr", "bornes", "aucun"),
  iqr_facteur = 3,
  prix_min = NULL,
  prix_max = NULL,
  strata = NULL,
  weights = NULL
)
}
\arguments{
\item{data}{Un data frame.}

\item{col_prix}{Nom d'une colonne de prix déjà disponible. Si fourni, le
prix est utilisé tel quel (mode "prix direct").}

\item{col_montant, col_quantite}{Noms des colonnes de montant et de quantité,
utilisés pour reconstituer le prix si \code{col_prix} est NULL.}

\item{methode_nettoyage}{"iqr" (défaut), "bornes" ou "aucun".}

\item{iqr_facteur}{Multiple de l'écart interquartile (défaut 3).}

\item{prix_min, prix_max}{Bornes absolues (si methode = "bornes").}

\item{strata}{Nom(s) de colonne(s) d'agrégation. Si NULL, prix par ligne.}

\item{weights}{Colonne de poids pour l'agrégation. Si NULL, moyenne simple.}
}
\value{
Si \code{strata} est NULL, le data frame avec \code{prix_unitaire}.
Sinon, un data frame agrégé par strate (\code{prix_moyen}, \code{n}).
}
\description{
Deux modes : (1) reconstitution du prix à partir d'un montant et d'une
quantité (\code{col_montant} et \code{col_quantite}), utile quand seules les
transactions sont connues ; (2) prix déjà relevé fourni directement via
\code{col_prix}. Dans les deux cas, un nettoyage des aberrations et une
agrégation pondérée par strate peuvent être appliqués.
}
